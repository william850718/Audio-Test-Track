# 開發週報 2026-W31

**期間** 2026-07-27 ~ 2026-08-02(台北時間)

> 由 git commit 自動產生,分類為關鍵字推斷,僅作為整理素材。
> 要給主管的版本請另行改寫成中文、以使用者影響為主的敘述。

## 摘要

- commit 數:**10**
- 程式碼變動:**+1199 −290**(3 個檔案)
- 分類:新功能 3 · 調整與優化 4 · 其他 3

## 新功能

### Lab Instruments: create categories up front, with Teams / Chrome Audio defaults
`cbf097c` · 2026-07-31

Categories previously only came into being as a side effect of typing one into an instrument row, which made setting them up awkward. They are now first-class.

### Add Lab Instruments mode (per-lab instrument/version inventory)
`96b2521` · 2026-07-28

New third mode alongside Platform Tracking and Project Log: a per-lab inventory of instruments and software versions with periodic-check tracking.

### Add file attachments (Office/PDF/zip) and image-size reminders
`99d63b3` · 2026-07-27

Records and project logs can now attach non-image files alongside screenshots, reusing the existing public attachments bucket (no SQL change needed).

## 調整與優化

### Move record details into a modal; version history as a changelog
`f2a1c8f` · 2026-07-31

Reworks the version feature so the list stays clean and the history reads like a commit log.

### Keep previous iterations as version history on a record
`8c1779a` · 2026-07-31

Records can now retain their previous content instead of overwriting it, so a document that goes v1 -> v2 -> v3 stays traceable.

### Lab Instruments: rename columns to Version / Latest update
`e074a02` · 2026-07-29

The "Value" and "Version" columns were redundant (both showed the same string). Rename them so each carries distinct meaning:

### Lab Instruments: tidy the checked/version labels
`a0e07f3` · 2026-07-28

- Table header shows just "Version" (the per-row change date stays below each value). - Shorten the freshness pill to "Checked <date> - Nd ago" and force it onto one line (white-space: nowrap) so it no longer wraps; move the "by <user>" byline out of the pill into a separate muted span. - Drop the "review" suffix from the label; the red colour already signals a lab is overdue.

## 其他

### Lab Instruments: per-lab categories to split the list into sections
`b27d18f` · 2026-07-31

Each lab can now group its instruments under its own free-form categories, so e.g. Teams gear and Zoom gear are recorded separately in one lab.

### Lab Instruments: explicit reorder mode for the instrument list
`9586e7b` · 2026-07-31

Lets each lab arrange its instruments in a meaningful order without exposing drag targets during normal use.

### Lab Instruments: per-row editing instead of whole-table edit
`96b37dc` · 2026-07-28

Replace the global "Update" mode (which turned every row into inputs at once) with per-row inline editing:

---

<details><summary>完整 commit 清單</summary>

- `cbf097c` 2026-07-31 — Lab Instruments: create categories up front, with Teams / Chrome Audio defaults
- `b27d18f` 2026-07-31 — Lab Instruments: per-lab categories to split the list into sections
- `9586e7b` 2026-07-31 — Lab Instruments: explicit reorder mode for the instrument list
- `f2a1c8f` 2026-07-31 — Move record details into a modal; version history as a changelog
- `8c1779a` 2026-07-31 — Keep previous iterations as version history on a record
- `e074a02` 2026-07-29 — Lab Instruments: rename columns to Version / Latest update
- `a0e07f3` 2026-07-28 — Lab Instruments: tidy the checked/version labels
- `96b37dc` 2026-07-28 — Lab Instruments: per-row editing instead of whole-table edit
- `96b2521` 2026-07-28 — Add Lab Instruments mode (per-lab instrument/version inventory)
- `99d63b3` 2026-07-27 — Add file attachments (Office/PDF/zip) and image-size reminders

</details>
