# 開發週報 2026-W32

**期間** 2026-08-03 ~ 2026-08-09(台北時間)

> 由 git commit 自動產生,分類為關鍵字推斷,僅作為整理素材。
> 要給主管的版本請另行改寫成中文、以使用者影響為主的敘述。

## 摘要

- commit 數:**8**
- 程式碼變動:**+650 −67**(3 個檔案)
- 分類:新功能 1 · 問題修正 2 · 調整與優化 4 · 其他 1

## 新功能

### Require admin approval before a new account can use the app
`25c8ccc` · 2026-08-06

Sign-up previously granted full access as soon as the email code was verified: every policy was "to authenticated using (true)" and the company-domain check lived only in the browser, so any account could read and write everything.

## 問題修正

### Fix the off-centre admin button icon
`59fdd3c` · 2026-08-06

The button carried a stray space and an always-present empty span for the pending count, which pushed the glyph off centre and made it sit differently from the bell and theme buttons beside it. It is icon-only again (and uses the tidier ti-users glyph); the count is now a corner badge that is hidden at zero, so the button keeps the same 41x28 footprint either way.

### Lab Instruments: fix category count placement and cramped row actions
`556eca3` · 2026-08-03

- The per-category count was pushed to the right edge of the section header, where it landed under the Latest update column and read like a value in that column. It now sits directly after the category name, and the header actions are what get pushed right (aligning with the row actions below them). - Row columns were still sized for two action icons, so the third made the Latest update column and the icons collide. Widened the action column to fit three icons and increased the column gap; the reorder and edit rows were resized to match their own icon counts. - On narrow screens rows now stack into a single column with the actions on their own line, instead of squeezing a vertical icon stack beside the text.

## 調整與優化

### Make every Platform Tracking filter one component, and fix a blank label
`63889b5` · 2026-08-07

The bar mixed two implementations: Category and Type were custom dropdowns (30px) while Sample Types and Status were native selects (36px), so they sat at different heights and expanded into differently styled lists.

### Keep the bootstrap-admin email as a placeholder in the approval SQL
`82f0001` · 2026-08-06

The repo is public, so the first-admin address must not be committed. Restore the CHANGE-ME placeholder and expand the comment to say why, plus how to add another admin later with a single update statement.

### Lab Instruments: drop the per-category item count from section headers
`c2b3104` · 2026-08-03

### Lab Instruments: move an existing instrument between categories directly
`23c82c5` · 2026-08-03

Refiling an instrument previously meant opening the row editor and retyping the category, which was the long way round. Two direct paths now exist.

## 其他

### Platform Tracking: fly-out sub-filters and a fixed filter order
`8d132c1` · 2026-08-07

The Category and Type filters each had a second dropdown that only appeared once the parent was chosen, so the filter bar changed width and the controls after it kept shifting. Both are now single menus with one level of children.

---

<details><summary>完整 commit 清單</summary>

- `63889b5` 2026-08-07 — Make every Platform Tracking filter one component, and fix a blank label
- `8d132c1` 2026-08-07 — Platform Tracking: fly-out sub-filters and a fixed filter order
- `59fdd3c` 2026-08-06 — Fix the off-centre admin button icon
- `82f0001` 2026-08-06 — Keep the bootstrap-admin email as a placeholder in the approval SQL
- `25c8ccc` 2026-08-06 — Require admin approval before a new account can use the app
- `c2b3104` 2026-08-03 — Lab Instruments: drop the per-category item count from section headers
- `556eca3` 2026-08-03 — Lab Instruments: fix category count placement and cramped row actions
- `23c82c5` 2026-08-03 — Lab Instruments: move an existing instrument between categories directly

</details>
