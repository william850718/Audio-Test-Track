# AudioTracker 上線前測試清單

> 建議在「測試時段」進行，找 2 個人（或 2 台電腦 / 2 個瀏覽器）一起測。
> 重點是驗證「多人不會互相蓋資料」——這是核心。

## A. 環境設定（先做，一次性）
- [ ] Supabase SQL Editor 執行 `aclab-normalize-setup.sql`，Success 無紅色錯誤
- [ ] 執行 `aclab-storage-setup.sql`（截圖功能）
- [ ] 執行 `aclab-settings-setup.sql`（Test Software 清單）
- [ ] 執行 `aclab-push-setup.sql`（推播訂閱表）
- [ ] 執行 `aclab-lab-instruments-setup.sql`（實驗室儀器清單）
- [ ] Database → Replication 看得到 `records` / `projects` / `logs` / `app_settings` / `lab_instruments`
- [ ] Storage 看得到 `attachments` bucket，且為 Public

## B. 資料搬遷（第一次登入）
- [ ] 登入後右上角同步狀態顯示 **Synced**（不是紅色 Sync failed）
- [ ] 舊的記錄 / 專案 / Log **全部都在**、數量正確
- [ ] Table Editor 裡 `records` / `projects` / `logs` 有資料
- [ ] 舊的 `app_snapshot` 表仍在（備份，沒被刪）

## C. 單人基本功能
### Platform Tracking
- [ ] 新增記錄 → 出現在清單、Synced；存檔後跳「Record added」提示
- [ ] Record Type 選 **Note** → 出現 Sample Type，可**複選**多種樣品
- [ ] **Sample Type 依平台+分類變動**（Note 類型下逐一確認）：
  - [ ] Teams（任一分類）→ Personal Headset USB/Dongle/NBT-Classic/NBT-LE Audio、Personal Speakerphone、**Personal Deskphone**、**Personal Laptop**、Shared Speakerphone
  - [ ] Zoom → **Audio**：Zoom Meeting / Zoom Rooms；**AIO (Function)**：ZRC / ZRP / FW&AIO
  - [ ] Microsoft HDE → **Cortana**：Headset / Laptop / Center of room / Edge of room；**Talk Analysis Suite**：整段不顯示
  - [ ] Intel → **SPET**：LNL / PTL；**BT Audio**：整段不顯示
  - [ ] Google → **Chrome Audio**：Chromebox / Chromebook system / ALOS system / Soundcheck v.20 / v.21；其他分類不顯示
  - [ ] Lenovo → Platform Category 有 **LNV / HWF**，Sample Type 整段不顯示；新增記錄 ID 為 `LN-xxx`
- [ ] 切換平台或分類時，不屬於新清單的舊勾選會被清掉（不會殘留別的平台的選項）
- [ ] **開啟舊記錄編輯**：原本勾的樣品類型仍在（即使該選項已不在新清單中），存檔後不遺失
- [ ] **舊 Zoom 記錄**（分類為 Zoom meeting / Zoom Room / Zoom FW Test）編輯時分類顯示為「（舊分類）」且存檔後不被清空
- [ ] 上方 Sample Type 篩選器隨平台頁籤 / 分類變動；Lenovo 等沒有樣品類型時整個篩選器隱藏
- [ ] Record Type 選 **Update** → Update Type 選 **Firmware** → 出現 **Test Software** 下拉（含 Other）
- [ ] Test Software 旁「⚙ manage」可新增/刪除軟體，且會雲端同步
- [ ] 上傳截圖：多張、大圖自動壓縮、**總量超過 5MB 被擋**；卡片直接顯示、點擊放大
- [ ] 上傳截圖時：選到 **≥3MB 的大圖** → 跳「已自動壓縮」提示；上傳區顯示「大圖會自動壓縮」提醒文字
- [ ] **夾帶檔案（Attachments）**：可加 Excel/PDF/Word 等 → 卡片顯示可下載標籤（含檔案圖示、大小），點擊可開啟/下載
  - [ ] 擋 **不允許的類型**（如 .exe）→ 跳警告
  - [ ] 擋 **單檔 >10MB** 與 **總量 >25MB** → 跳警告
  - [ ] 編輯既有記錄：原本的檔案仍在、可移除、可再新增；存檔後正確
- [ ] 刪除有截圖 **或檔案** 的記錄後，Storage 的 `attachments` 對應圖片/檔案都被清掉
- [ ] **卡片操作**：滑鼠移到卡片上，右上角浮現圖示 → Edit / Duplicate / Close / Delete 都正常
- [ ] **Duplicate**：以某筆為範本開新增表單（帶入平台/類別/軟體，清空標題與截圖）
- [ ] **點卡片開啟詳細視窗**：顯示完整內容＋操作鈕（Edit / Duplicate / Close / Delete）；按 Esc 或點背景可關閉
  - [ ] 點卡片上的**圖示、圖片、檔案連結**時**不會**跟著打開詳細視窗（各做各的事）
- [ ] **版本歷史（GitHub 式變更紀錄）**：Edit → 下方「Save as」選 **Save as new version** → 出現「What changed?」→ 填寫後存檔
  - [ ] **沒填變更說明會被擋下**，不會存成版本
  - [ ] 卡片的 ID 旁出現 **v2** 小標（不在 Teams 那排徽章裡）；**卡片本身不顯示歷史**，保持清爽
  - [ ] 詳細視窗裡的 **Version history 時間軸**：新的在上（v3→v2→v1），v1 標示為 Original version
  - [ ] 選 **Update this record**（預設）改錯字**不會**產生版本
  - [ ] 版本只記文字說明，**不保留當時的附件**（附件永遠只有最新一份）
  - [ ] CSV 匯出最後一欄 Version 顯示 v1/v2/v3
- [ ] 卡片版面：標題一行、徽章一列、狀態正常；顯示「最後修改者」
### Project Log
- [ ] 新增專案 → 新增 Daily Log（含截圖）→ 正常顯示
- [ ] 編輯 / 刪除 Log、關閉 / 重開 / 刪除專案 都正常
### Lab Instruments（實驗室儀器）
- [ ] 上方第三個頁籤 **Lab Instruments** → 列出 A1~A10 共 10 間；備註（TAS/Zoom…）正確顯示
- [ ] 卡片顏色 = **距上次確認 (checked)** 的時間：≤30天綠、≤90天黃、>90天紅、從未確認灰
- [ ] 明細頁同時顯示兩個日期：**Last checked**（綠燈依據）＋ **Last version change**（僅資訊，不驅動燈號）
- [ ] 每列右側有 **編輯 / 刪除** 小圖示；底部有「Add instrument」按鈕
- [ ] **單列編輯**：點鉛筆 → 只有那一列變輸入框（其他列不動）；改值時版本即時預覽 → ✓ 存檔；只有這列的變更日期更新（Enter 存、Esc 取消）
- [ ] **Add instrument**：底部按鈕 → 出現一列空白 → 填名稱/值 → ✓ 新增
- [ ] **刪除單列**：垃圾桶 → 跳確認框 → 確認後移除
- [ ] **調整順序**：平常畫面**完全沒有拖曳把手**（不會誤觸）→ 按「Reorder」才進入
  - [ ] 進入後出現把手與上下箭頭、紫色提示列；「Add instrument」與編輯/刪除圖示暫時隱藏
  - [ ] 電腦：**抓住把手**可拖曳（拖文字不會動），放開時有藍紫色落點提示
  - [ ] 手機：用**上下箭頭**調整（手機不顯示把手）；第一列的↑與最後一列的↓為灰色
  - [ ] 按 **Cancel** → 順序回復原狀，什麼都沒存
  - [ ] 按 **Done** → 順序存檔，且 **Checked 日期與各列的 Latest update 都不會被更動**（純排版不算重新確認）
  - [ ] 另一個人重新整理後看到相同順序（順序是共用的）
- [ ] 正在編輯/新增某列時，其他列的圖示與「Add instrument」會暫時停用（一次只編一列）
- [ ] **Mark checked**：跳確認框「確認無誤?」→ 按確認 → 只把 checked 日期更新為今天、燈回綠，**不動任何資料**（這就是「沒更新也能消紅燈」的機制）
- [ ] **第一次建檔貼上**：只在該實驗室**還空的時候**才出現；貼 `Audio Lab.txt` 內容 → Preview 解析各列 → 「Apply to list」填入並存檔；建好後貼上區消失（避免日常誤觸）
- [ ] 兩人同時：A 更新某實驗室 → B 幾秒後自動看到（Realtime）

## D. 篩選・排序・儀表板・匯出
- [ ] 搜尋框打字順暢（debounce，不頓）
- [ ] 篩選：All Categories / Types / Status / **Sample Types** 可疊加
- [ ] 排序（右側 ⇅ 圖示）：Newest / Oldest / 依更新 / 截止日 / 標題 / 平台，切換正確
- [ ] **同一天**建立的多筆記錄，Newest 依序正確（新的在前，不亂）
- [ ] Dashboard：Total / In Progress / Closed / 平均天數 + 長條圖；點卡片可篩選
- [ ] Export：Platform 與 Project 各能匯出 **Excel(CSV)** 與 **PDF**（含 Test Software 欄）
- [ ] 淺色 / 深色切換（右上角 ☀/🌙）：兩種主題文字都清楚、按鈕顏色正常

## E. 多人協作（核心！至少 2 人同時）
- [ ] **不互相覆蓋**：A、B 同時開著。A 新增，B 幾秒後**自動出現**（不用重整）
- [ ] **舊 bug 已解**：A 開著舊畫面 → B 新增 → A 改別筆並存 → **B 那筆不會消失**
- [ ] **不撞號**：A、B 幾乎同時各新增同平台記錄 → 兩筆都在、ID 不重複
- [ ] 同一筆兩人同時改 → 後存的勝出（可接受），其他記錄不受影響

## F. 離線 / 例外
- [ ] 斷網操作 → 狀態顯示「Offline — N pending sync」，資料仍在畫面上
- [ ] 恢復網路（或重新登入）→ 待送的變更**自動補送**、狀態回 Synced
- [ ] Export 下載 JSON 備份正常；Import 還原正常

## G. 推播通知（需先完成 Supabase 推播設定 + VAPID 金鑰）
- [ ] index.html 的 `VAPID_PUBLIC_KEY` 已填、`sw.js` 已在網站根目錄
- [ ] 右上角 🔔 → 允許通知 → 跳「Notifications enabled」
- [ ] 另一人（或另一裝置）新增一筆記錄 → 幾秒內收到系統通知
- [ ] 點通知 → 開啟/切回 AudioTracker
- [ ] iPhone：需先「加入主畫面」當 App 用，才收得到

## H. 手機
- [ ] 手機登入、看記錄、上傳截圖、版面正常
- [ ] 卡片操作圖示在手機上常駐可按（無 hover 也能用）

---
**判讀**：E 區全過 = 多人安全，可上線。G 區要等推播設定完成才測。
任何紅色錯誤或資料異常，先別上線，把畫面/訊息回報給我。
